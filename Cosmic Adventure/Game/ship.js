class Ship
{
  constructor(x, y, w, h, controls, image, id = 0, speed = 0, direction = 0)
  {
    this.x = x
    this.y = y
    this.w = w
    this.h = h

    this.id = id
    this.speed = speed
    this.direction = direction
  }
}
